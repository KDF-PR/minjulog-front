# @angular/fire 가 아직 Angular 22 를 지원하지 않는다.
# 최신 릴리스(20)는 Angular 20, next(21.0.0-rc.0)는 Angular 21 을 peer 로 요구해
# 이 설정이 없으면 npm install 이 ERESOLVE 로 멈춘다.
#
# 빌드와 런타임은 Angular 22 에서 정상 동작하는 것을 확인했다.
# @angular/fire 가 Angular 22 를 지원하면 이 파일을 지운다.
legacy-peer-deps=true
